# BPE Training Configuration
# Used by train_bpe.sh. Override via TRAIN_BPE_ENV_FILE=/path/to/custom.env

BPE_TRAIN_MODULE="cs336_basics.bpe"

# === Single run mode (choose one) ===
# Option 1: Use predefined dataset name
# BPE_DATASET="tinystories_valid"
# BPE_VOCAB_SIZE=""  # optional override

# Option 2: Use custom file path
# BPE_INPUT_PATH="data/custom.txt"
# BPE_VOCAB_SIZE="5000"  # required with INPUT_PATH

# === Batch run mode ===
# Run multiple datasets (comma-separated dataset:vocab pairs)
BPE_RUNS="tinystories_train:10000,owt_train:32000"

# === Optional parameters ===
BPE_NUM_WORKERS=""     # Auto-detect if empty
BPE_OUTPUT_DIR=""      # Same as input dir if empty
BPE_LOG_FILE=""        # Auto-generated in batch mode (logs/bpe_<dataset>_<vocab>_<timestamp>.log)
                       # For single run: empty = stdout only, or set to "logs/bpe_training.log"
BPE_SAVE="true"        # Set "false" to skip saving (default: save)

# === Cloud instance settings ===
BPE_AUTO_SHUTDOWN="true"  # Auto-shutdown after completion
